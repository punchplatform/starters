[tool.poetry]
name = "custom_node"
# managed by makefile
version = "0.0.0"
description = ""
authors = ["Punch"]

[tool.poetry.dependencies]
python = ">=3.6.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
